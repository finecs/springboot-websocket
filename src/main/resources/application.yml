# 数据库
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/chat
    username: root
    password: fanying2015
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  # 文件上传下载
  servlet:
    multipart:
      enabled: true
      max-file-size: 1024MB
      max-request-size: 1024MB
      file-size-threshold: 2KB
file:
  dir: ./files


